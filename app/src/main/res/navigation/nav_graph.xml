<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/contactsFragment">

    <fragment
        android:id="@+id/contactsFragment"
        android:name="com.alexP.socialnetwork.ui.screens.contacts.ContactsFragment"
        android:label="fragment_contacts"
        tools:layout="@layout/fragment_contacts" >
        <action
            android:id="@+id/action_contactsFragment_to_contactsDetailsFragment"
            app:destination="@id/contactsDetailsFragment" />
    </fragment>
    <fragment
        android:id="@+id/contactsDetailsFragment"
        android:name="com.alexP.socialnetwork.ui.screens.contactsdetails.ContactsDetailsFragment"
        android:label="fragment_contact_details"
        tools:layout="@layout/fragment_contact_details" >
        <argument
            android:name="photo"
            app:argType="string" />
        <argument
            android:name="full_name"
            app:argType="string" />
        <argument
            android:name="address"
            app:argType="string" />
        <argument
            android:name="career"
            app:argType="string" />
    </fragment>
</navigation>